const circleX = 50;

function setup() {
  createCanvas(600, 400);
}

function draw() {
  // background
  background(250, 250, 100);

  // The circle
  fill(250, 200, 200);
  ellipse(circleX, 200, 80, 80);
}
